import { createRoot } from "react-dom/client";
// TEMP: Render the useState assignment.
// Switch back to the CV by changing the import to: `import App from "./App.jsx";`
import App from "./useStateExercise/UseStateExercise.jsx";

createRoot(document.getElementById("root")).render(<App />);
